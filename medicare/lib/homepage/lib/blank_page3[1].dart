import 'package:flutter/material.dart';

class BlankPage3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Blank Page 2'),
      ),
      body: const Center(
        child: Text('This is Blank Page 3.'),
      ),
    );
  }
}
